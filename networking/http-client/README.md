# HTTP Client
## What belongs here
Use this folder for future REST-call practice, synchronous vs asynchronous request handling, and client configuration notes.
## Java sheet mapping
- HTTP Client
- HttpClient
- HTTP Methods
- Async HTTP
- sendAsync() + CompletableFuture
## Future practice files
- Add focused exercises, notes, and experiments that directly practice the mapped concepts listed above.
- Prefer one concept-rich exercise per file when this folder starts receiving Java sources later.
- Keep trivial theory-only leaves documented in this README instead of creating extra folders too early.
## Grouping note
Async HTTP remains a section here because it is a mode of using the same client API.
