# Daemon and Naming
## What belongs here
Use this folder for future exercises around thread configuration, factories, and failure handling.
## Java sheet mapping
- Daemon and naming
- Daemon threads
- Thread naming
- ThreadFactory
- UncaughtExceptionHandler
## Future practice files
- Add focused exercises, notes, and experiments that directly practice the mapped concepts listed above.
- Prefer one concept-rich exercise per file when this folder starts receiving Java sources later.
- Keep trivial theory-only leaves documented in this README instead of creating extra folders too early.
## Grouping note
These concepts are grouped because they shape thread identity and runtime behaviour rather than core synchronisation.
